# Audit-Lauf RUN_2026-04-07 (Prompt A — Wiederholung)

## Umgebung

- Host: Windows, PowerShell
- Repo: `bitget-btc-ai`
- **Branch:** `master`
- **HEAD:** `f09221a47f834388232358ab66d5f8f616d95995` (`git rev-parse HEAD`)
- **Letzter Commit:** `docs(audit): Sprint 1 evidence commit hash + backlog/scorecard sync`
- **Working tree:** clean (`git status -sb` → `## master`)

## Ausgefuehrte Checks (dieser Lauf)

```text
git branch --show-current  → master
git rev-parse HEAD           → f09221a47f834388232358ab66d5f8f616d95995
git status -sb               → ## master (clean)
```

```text
# CI-Platzhalter wie in .github/workflows/ci.yml (sed <SET_ME>)
python tools/validate_env_profile.py --env-file <TEMP>/ci-audit.env.local --profile local
→ OK validate_env_profile: local ...
```

```text
pnpm check-types (turbo)
→ 2 packages successful (@bitget-btc-ai/shared-ts, @bitget-btc-ai/dashboard)
```

```text
docker compose -f docker-compose.yml config --quiet
→ Exit 0 (Konfiguration syntaktisch gueltig)
```

## Nicht ausgefuehrt (Explizit)

- `docker compose up` / Service-Logs / Health-URLs gegen laufende Container
- `pnpm e2e` / Playwright HTML/JUnit-Artefakt-Einsammeln auf diesem Host
- `pnpm llm:eval` / `pytest tests/llm_eval`
- Vollstaendiger UI-Link-Crawl ausserhalb der dokumentierten Playwright-Specs

## Statische Code-Hinweise (Silent-Fail-Klassen)

- `res.json().catch(() => ({}))` / `res.text().catch(() => "")` in Dashboard (parsen fehlgeschlagener Bodies) — **akzeptiert** wenn danach explizite Fehlerzweige folgen; weiterhin pruefen: ob Nutzer immer eine Meldung sieht.
- Leeres `.catch(() => {})` in `apps/dashboard/src`: **keine Treffer** im Grep-Lauf dieses Datums (Pattern `.catch(() => {`).

## Verweise

- E2E-Specs: `release-gate.spec.ts`, `trust-surfaces.spec.ts`, `responsive-shell.spec.ts`, `broken-interactions.spec.ts`
- Fruehere Laeufe: `RUN_2026-04-08.md`, `RUN_SPRINT1_2026-04-08.md`
