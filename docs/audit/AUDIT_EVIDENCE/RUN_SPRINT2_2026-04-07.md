# Sprint 2 (Prompt B) — Evidence — 2026-04-07

## Scope (umgesetzt)

- Marktuniversum-Seite: **Daten-Lineage-Panel** (`MarketUniverseDataLineagePanel`, `data-testid="market-universe-lineage"`).
- **Pagination:** `universePage` (32 Symbole/Seite), `registryPage` (40 Instrumente/Seite), Query bleibt mit `symbol`/`timeframe`/`diagnostic` verknuepft.
- **Tests:** `apps/dashboard/src/lib/__tests__/market-universe-lineage.test.ts`; E2E `release-gate.spec.ts` neuer Fall „market-universe — Daten-Lineage Panel“.

## Befehle (lokal)

```text
pnpm check-types  → gruen
pnpm exec jest src/lib/__tests__/market-universe-lineage.test.ts --runInBand
(Working directory: apps/dashboard)
→ 4 Tests gruen
```

## Nicht ausgefuehrt hier

- `pnpm e2e` (benoetigt laufendes Dashboard)
- `docker compose up` / `rc:health`
