# Sprint 1 (Prompt B) — Evidence

**Datum:** 2026-04-08  
**Git-Commit:** `54f3917b647ff65748cecf35c720b38d1ad61005` (Branch `master`)  
**Scope:** Baseline-Doku, `validate_env_profile.py --help` (Windows cp1252), Locale-Mirror ohne stilles `catch`, E2E `broken-interactions.spec.ts`, JUnit-Reporter.

## Befehle (lokal ausgefuehrt)

```text
python tools/validate_env_profile.py --help
→ Exit 0 (nach ASCII-Ersetzung von Pfeilen/Gedankenstrichen in Tool-Strings)
```

```text
pnpm check-types
→ turbo: @bitget-btc-ai/dashboard + shared-ts erfolgreich
```

```text
pnpm exec jest src/lib/client/__tests__/best-effort-fetch.test.ts --runInBand
(Working dir: apps/dashboard)
→ 2 Tests gruen
```

## E2E (Playwright)

- Neue Spec: `e2e/tests/broken-interactions.spec.ts` (Sidebar-Link-Traversal + `/`, `/welcome`).
- **Hinweis:** Vollstaendiger Lauf `pnpm e2e` erfordert laufendes Dashboard (z. B. CI-Job `compose_healthcheck` mit `E2E_BASE_URL=http://127.0.0.1:3000`).
- JUnit-Ausgabe (bei Lauf): `e2e/test-results/junit.xml` — Ordner in `.gitignore`; Artefakt in CI hochladen optional.

## Artefakte

| Datei | Zweck |
|-------|--------|
| `docs/audit/SPRINT_PLAN.md` | Sprint 1–5 Plan |
| `docs/audit/BRANCH_AND_COMMIT_POLICY.md` | P0-1 Richtlinie |
| `e2e/playwright.config.ts` | + junit reporter |
