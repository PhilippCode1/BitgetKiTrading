# OpenAPI (API-Gateway)

Datei `api-gateway.openapi.json` wird generiert:

```bash
python scripts/export_openapi.py
```

Nach Routen-/Model-Aenderungen am Gateway neu erzeugen und committen, damit Typgeneratoren und Reviews dieselbe Referenz nutzen.
